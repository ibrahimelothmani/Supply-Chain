apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: supplychain
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: ghcr.io/ibrahimelothmani/inventory-backend:744fb512b6de21424d8f54d5f20910e1339b9146
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres.supplychain.svc.cluster.local:5432/inventory_db
        - name: SPRING_DATASOURCE_USERNAME
          value: postgres
        - name: SPRING_DATASOURCE_PASSWORD
          value: postgres
