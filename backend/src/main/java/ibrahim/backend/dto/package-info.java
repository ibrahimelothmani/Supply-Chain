/**
 * This package contains Data Transfer Objects (DTOs) used for API request and response payloads.
 * Entities must never be exposed directly through the API — always use DTOs.
 */
package ibrahim.backend.dto;
